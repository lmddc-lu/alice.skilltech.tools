# angularx-qrcode has no Angular 22 release yet (latest 21.0.5, peer ^21).
# It is compiled against Angular 21 and runs fine on the Angular 22 app;
# only npm's strict peer check objects. Remove this once angularx-qrcode supports v22.
legacy-peer-deps=true
